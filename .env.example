# Mapbox (for /mapbox-test page). Get token: https://account.mapbox.com/access-tokens/
VITE_MAPBOX_ACCESS_TOKEN=

# Optional: Studio에서 저장한 스타일 URL (없으면 기본 스타일 사용)
# Mapbox Studio → Styles → 스타일 선택 → Share → Style URL 복사
# VITE_MAPBOX_STYLE_URL=mapbox://styles/your-username/your-style-id

# Optional: Studio에서 Publish 후 스타일이 바로 안 보일 때. 숫자 바꿔가며 새로고침하면 캐시 무시됨 (최대 15분 걸릴 수 있음)
# VITE_MAPBOX_STYLE_CACHE_BUST=1
